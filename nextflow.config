/*
 * Pipeline parameters (defined in config, no nextflow_schema.json needed)
 * These values can be overridden at launch time (e.g. --greeting "Hi")
 */
params {
  greeting = 'Hello'
  name     = 'ICA'
  repeat   = 3
  outdir   = 'out'
}
